# Smart Assistant — Python 后端依赖
# 从 pyproject.toml 生成，Docker 构建用
# 版本范围与 pyproject.toml 保持一致

# AI 框架
langchain>=0.3.0
langchain-openai>=0.3.0
langgraph>=0.4.0

# 数据存储
chromadb==0.6.3
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.30.0
aiosqlite>=0.20.0
redis>=5.0.0

# Web 框架
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
sse-starlette>=2.0.0
python-multipart>=0.0.12

# 配置 & 数据模型
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# 日志 & 工具
loguru>=0.7.0
tenacity>=9.0.0
aiofiles>=24.0.0

# 邮件
aiosmtplib>=3.0.0

# 文档处理
pypdf>=5.0.0
python-docx>=1.0.0
openpyxl>=3.0.0

# 数据库迁移
alembic>=1.14.0

# 限流 & 监控
slowapi>=0.1.9
prometheus-fastapi-instrumentator>=7.0.0
